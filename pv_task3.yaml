apiVersion: v1
kind: PersistentVolume
metadata:
  name: p-vol1
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: sc
  hostPath:
    path: /tmp/date